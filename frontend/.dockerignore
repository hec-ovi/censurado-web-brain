# The console image (built from this dir) copies only nginx.conf and src/. Keep the
# rest out of the build context so `docker compose up --build` does not tar the whole
# node_modules tree to the daemon. This does not change the resulting image.
node_modules
coverage
test
package.json
package-lock.json
.gitignore
README.md
