news, which sources are used and how do we keep those to have a list of repeated/redundant news?
i am thinking to make per author a list of latest news sources used and harden avoid those, this means, how can we somehow save a hash of a specific url already fetched so does not write the same thing multiple times? i would recommend to a limited list of a CAP by entries, so lets say 50 entries top, and could be customized, but any link that is there, must be rejected and explained this new were already used. Again per author, find a way to do this, and test it with few examples, adding, and check automation removal (if we in 50 and we entry 1+ its added on top and removed oldest one).

About a full batch, this is per author, so i would recommend a whole entirely new workflow, similar to the article one:
I would recommend do like this, take the authors and NOT WRITTING ARTICLES but a lightweight provided list of titles with description of possible candidate articles per author.
This is temporally saved into the cache of that specific automated batch, once all authors finished writing their article candidate news (daily, recent, and can be from 1 to X, i would say 1 to 10, just careful here because we want to avoid 2 things, one the overfit like the agent saying "I MUST WRITE EXACLY 10 CANDIDATES" annd because following that strict mandate it forgets the main mandate "from all your sources, select top relevant candidates", so actually no limit, lets keep they find whatever is relevant so we ensure quality over formal formats).
Once that is all written, we go to the guy that generates the daily batch, and he goes and select, from all the authors, and all their candidates, what is REALLY relevant today? what should be portada and main article? which ones should have image? (herei  want to stop prompting authors with no images forced by author and instead allow the main agent orchestrator decide, e.g. conspiracy, art, AI or technology, we can generate/fabricate images, but... news like strong ones would sound off, so he shoudl decide with clear prompt).
Then once this orchestrator selects (the jefe de redaccion basically) he batches each article with its end to end workflow, and then organizes the portada order, etc, and then publishes the whole batch.
<- all this should be the automatic workflow that happens on batch daily, what do you think?

some additions of the batch, we have concurrency, so we can use this, maybe the articles can be written in parallel (does our automation support?) definitely openrouter should support it, samne as llama.cpp, also i have a doubt... the full batch or review, blah blah, the article workflow, THIS SHOULD HAPPEN IN A SINGLE SESSON WITH MEMORY satep by step, so it keeps MUCH MORE context, if this is not like this, it should, and in the case of the batch, the automatic triggered batch should also have memory, BUT not PER ARTICLE, but yes the candidates, the layout, etc and should ber able only to read title and descritpion for the articles to save context and be more focus.