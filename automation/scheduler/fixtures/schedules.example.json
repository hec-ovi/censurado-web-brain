{
  "contractVersion": "1.0.0",
  "logDir": "../logs/scheduler",
  "agent": {
    "cmd": ["noob", "exec", "--yolo", "-p", "{prompt}"],
    "cwd": "../..",
    "timeoutMs": 5400000
  },
  "schedules": [
    {
      "name": "daily-sweep",
      "at": "07:00",
      "prompt": "Read cli/SKILL.md and do a daily sweep of the latest news: python3 cli/censurado.py step --mode daily. Work the walk to completion; do not publish to production."
    },
    {
      "name": "last-hour",
      "every": "2h",
      "prompt": "Read cli/SKILL.md and run the last-hour lane: python3 cli/censurado.py step --mode last-hour. Work the walk to completion; do not publish to production."
    }
  ]
}
