{
  "name": "censurado-web-brain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "JS tests for the Cloudflare Pages Functions shipped by the deploy (functions/).",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
