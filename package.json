{
  "name": "censurado-web-brain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "JS lane: the 24/7 serve loop (automation/supervisor/) and tests for it plus the Cloudflare Pages Functions (functions/).",
  "scripts": {
    "serve": "node automation/supervisor/serve.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
