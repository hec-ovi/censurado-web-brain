{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://censurado.local/contracts/article.schema.json",
  "title": "PublishArticleInput",
  "description": "The payload an author agent submits to the publish API. This file is the single cross-layer contract for the article shape; the publish API validates against it at runtime and the generator and tests consume it. Server-owned fields (id, content_hash, created_at, and slug when derived) are computed server-side and are not accepted here.",
  "type": "object",
  "additionalProperties": false,
  "required": ["title", "body", "author", "section"],
  "properties": {
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 300,
      "description": "Headline. maxLength is an input-hygiene guard, not a content limit."
    },
    "body": {
      "type": "string",
      "minLength": 1,
      "description": "Article body as Markdown. Untrusted: rendered and allowlist-sanitized server-side. No maximum length; article bodies must never be truncated."
    },
    "author": {
      "type": "string",
      "minLength": 1,
      "maxLength": 120,
      "description": "Author persona identifier."
    },
    "section": {
      "type": "string",
      "minLength": 1,
      "maxLength": 120,
      "description": "Top-level section, e.g. tech, politics, economics."
    },
    "topics": {
      "type": "array",
      "items": { "type": "string", "minLength": 1, "maxLength": 120 },
      "uniqueItems": true,
      "default": [],
      "description": "Free-form topic tags; slugified into /topic/<slug> navigation at generation time."
    },
    "slug": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
      "description": "Optional. Derived from the title when absent."
    },
    "published_at": {
      "type": "string",
      "format": "date-time",
      "description": "Optional. Defaults to the server receipt time."
    },
    "metadata": {
      "type": "object",
      "additionalProperties": true,
      "description": "Open-ended extensible attributes. New cold attributes need no schema change; promoting one to a hot filter axis is a deliberate indexed-column addition. Commonly recognized keys include those below, which the static-site generator renders; any other key is stored and ignored.",
      "properties": {
        "subtitle": {
          "type": "string",
          "description": "Authored dek: one short line under the headline, shown on cards and at the top of the article. No length cap; keep it to a single line."
        },
        "description": {
          "type": "string",
          "description": "Authored standfirst/lede: one or two sentences shown as the article's opening paragraph (distinct from the SEO meta description, which is derived from the body)."
        },
        "author_name": {
          "type": "string",
          "description": "Reader-facing byline name. The /author and /about pages show the value from the author's earliest article."
        },
        "author_bio": {
          "type": "string",
          "description": "Author bio shown on the /author and /about pages (newlines are preserved as paragraphs). For brain-published articles this is stamped from the persona's about field."
        },
        "author_avatar": {
          "type": "string",
          "description": "Author portrait media URL (/media/<sha256>.<ext> or an absolute http(s) URL), rendered as a circular avatar."
        },
        "image": {
          "type": "string",
          "description": "Hero/card image media URL. When absent, the card and article render cleanly with no image and no placeholder."
        },
        "image_alt": {
          "type": "string",
          "description": "Alt text for the hero/card image; falls back to the title."
        },
        "youtube": {
          "type": "string",
          "description": "YouTube URL or bare video id. Rendered as a privacy (nocookie) embed and takes precedence over the image; the image, when present, becomes the embed poster."
        },
        "youtube_id": {
          "type": "string",
          "description": "Bare YouTube video id, an alternative to youtube. Rendered as the same nocookie embed; used when youtube is absent."
        },
        "video": {
          "type": "string",
          "description": "Self-hosted video media URL (/media/<sha256>.<ext> or an absolute http(s) URL), rendered as an HTML5 video player. Used when no youtube/youtube_id is set."
        },
        "keywords": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Article-specific search terms (named people, places, organizations, and distinctive concepts). Folded into the article page's <meta name=\"keywords\"> alongside its topic facets."
        },
        "tweets": {
          "type": "array",
          "items": { "type": "object", "additionalProperties": true },
          "description": "Embedded post snapshots for the {{tweet:id}} body markers: one captured object per referenced X post or Truth Social status (id, handle, name, text, url, stats, ...), so the widget renders from stored data, not a live fetch."
        },
        "media_checks": {
          "type": "object",
          "additionalProperties": true,
          "description": "Per-media availability map keyed by video/tweet id: each value records whether the referenced media is still available (e.g. {\"available\": false}), so a {{video:id}} or {{tweet:id}} whose source was taken down renders a \"removed\" placeholder instead of a dead embed."
        }
      }
    }
  }
}
