# journalist / research (v0 placeholder)

Authored in Step 4. Plan-then-search: generate 3 to 6 sub-questions for the
assigned angle, search each, and record every factual claim as a ledger row
{claim, url, snippet, fetched_at}. Grounding first.

Bounds (in code): max_research_steps and a ledger-stall detector (a step that
adds zero new source rows counts as no progress). Just-in-time fetch, never a
speculative crawl.

No output-length cap.
